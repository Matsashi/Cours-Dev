import { BetterSurlignDirective } from './better-surlign.directive';

describe('BetterSurlignDirective', () => {
  it('should create an instance', () => {
    const directive = new BetterSurlignDirective();
    expect(directive).toBeTruthy();
  });
});
